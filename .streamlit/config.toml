#[theme]
# Dark base for a premium look
#base = "dark"

# Main background (deep navy, not black)
#backgroundColor = "#0F172A"

# Cards / containers (slightly lighter for depth)
#secondaryBackgroundColor = "#1E293B"

# Accent color (vibrant teal-green)
#primaryColor = "#22D3EE"

# Text color (soft off-white)
#textColor = "#E5E7EB"

# Font
#font = "sans serif"



[theme]
base = "light"

primaryColor = "#38BDF8"              # Sky blue buttons & highlights
backgroundColor = "#F9FBFD"           # Very light background
secondaryBackgroundColor = "#E8F4FD"  # Soft sky-blue cards
textColor = "#0F172A"                 # Dark readable text
font = "sans serif"
