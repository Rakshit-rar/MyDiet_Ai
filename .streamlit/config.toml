[theme]
# Base theme
base = "light"

# Soft background (not pure white)
backgroundColor = "#F7F9FC"

# Card / container background
secondaryBackgroundColor = "#FFFFFF"

# Primary accent (calm green – diet/health friendly)
primaryColor = "#5FB3A2"

# Text color (soft dark, not pure black)
textColor = "#2E3440"

# Font style
font = "sans serif"
